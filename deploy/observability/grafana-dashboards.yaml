apiVersion: 1
providers:
  - name: forge
    folder: Forge
    type: file
    options:
      path: /var/lib/grafana/dashboards
  - name: base
    folder: Base
    disableDeletion: false
    updateIntervalSeconds: 30
    type: file
    options:
      path: /var/lib/grafana/dashboards
