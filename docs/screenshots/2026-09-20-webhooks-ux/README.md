# Webhooks UI/UX visual QA

The screenshots show the page component with mocked API responses in an isolated
shell. They are visual regression evidence, not proof of live SSO or backend
delivery behavior. Views: webhooks, deliveries with details open, notifications.
Each view was captured at 375, 1920 and 2560 pixels in the light theme.
