server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;

  # REST API and Git Smart HTTP share the backend service.
  location /api/ { proxy_pass http://backend:22801; }
  location /git/ { proxy_pass http://backend:22801; }
  location / { try_files $uri $uri/ /index.html; }
}
